{
  "name": "cafeproject",
  "private": true,
  "description": "Tooling for the JL Cafe Project. The app itself is plain static files — this exists only so the tests have a browser to drive.",
  "scripts": {
    "test": "node tests/run.js",
    "snapshot": "node tests/snapshot.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
